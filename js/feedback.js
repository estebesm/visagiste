import {closeMain, openMain} from "./main.js";

const feedbackSection = document.querySelector('.feedback')

export const openFeedback = () => {
    // closeMain()
    // feedbackSection.classList.add('opened')
    // document.getElementById('main__contacts').style.display = 'none'
    // window.scrollTo({top: 0, behavior: 'smooth'})
}

export const closeFeedback = () => {
    // feedbackSection.classList.remove('opened')
    // document.getElementById('main__contacts').style.display = 'block'
    // openMain()
    // window.scrollTo({top: 0, behavior: 'smooth'})
}